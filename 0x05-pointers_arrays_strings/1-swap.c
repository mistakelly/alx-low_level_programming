#include "main.h"
/**
 * swap two integer - a function that swaps the values of two integers
 * @n: input
 * Return: *a = *b
 */
void swap_int(int *a, int *b);
{
	int m;

	m = *a;
	*a = *b;
	*b = m;

}
