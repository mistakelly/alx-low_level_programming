#include "main.h"
#include <stdio.h>

/**
 * multiplication - a function that multiplies two numbers
 * @x,y numbers to be multiplied
 * Return: x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
